git commands
when you make a change to a file you need to add that changed file 
git add .\filename
we need to write a commit message
git commit -m "commit message"
we push the changes
git push -u origin branchName
